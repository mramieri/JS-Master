//el OBJETO es la pieza PRINCIPAL en JS.

const nombre = "Monitor 20 pulgadas";
const precio = 300;
const disponible = true;

//un objeto agrupa TODO en una sola variable. el indicativo de objeto es el siguiente: "object literal":
//nombre: //"propiedad o llave del objeto"
// en las variables se usa el =. en los objetos se usan dos los puntos : . CLAVE

const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300, 
    disponible: true,
}

console.log(producto);

//si quiero agregar mas valores al objeto, lo agrego unicamente con una ,   