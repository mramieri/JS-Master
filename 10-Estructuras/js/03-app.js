//Operador mayor que o menos que

const dinero = 500;
const totalAPagar = 500;

if ( dinero >= totalAPagar ) { // Reminder: lo que está en () es la condición, y lo que está entre {} es el código que se ejecutaría en caso de que se cumpla la condición de los () y se evalúe como verdadera.
    console.log("si, podemos pagar");
} else {           //Peero si no se cumple la condición, entonces
    console.log("fondos insuficientes");
}