//108. Detectar cuando el HTML esta listo
//un evento es un like por ejemplo en twitter o un mg en fb
//y acá se pone el evento en cuestion

    console.log(1);
document.addEventListener('DOMContentLoaded', () => {
    console.log(2);
}); 


    console.log(3);

//El DOMContentLoaded se ejecuta una vez que se descarga toodo el html, una vez que 'el documento está listo'
